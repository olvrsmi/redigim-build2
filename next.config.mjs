/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // output: 'export',
  // distDir: 'imaginaries',
  // basePath: '/imaginaries',
};

export default nextConfig;
